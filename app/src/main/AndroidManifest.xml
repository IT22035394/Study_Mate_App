<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/Theme.MyApplication">

            <!-- Welcome page: the launcher -->
            <activity android:name=".WelcomePageActivity"
                android:exported="true">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>

            <!-- Destination page -->
            <activity android:name=".StudentTeacherSelectionActivity" />

            <!-- sign-up screen -->
            <activity android:name=".SignUpActivity" />

            <!-- login screen -->
            <activity android:name=".LoginActivity" />

            <!-- home page -->
            <activity android:name=".HomeActivity" />

        <!-- teacher's profile page -->
        <activity android:name=".TeacherProfileActivity" />


        <!-- video upload page -->
        <activity android:name=".UploadVideoActivity" />


    </application>

</manifest>
